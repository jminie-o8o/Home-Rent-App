oauth:
  github:
    client-id: ${GITHUB_CLIENT_ID}
    auth-server-url: ${GITHUB_AUTH_URL}
    resource-server-url: ${GITHUB_USER_URL}
    secret-key: ${GITHUB_SECRET_KEY}
  kakao:
    client-id: ${KAKAO_CLIENT_ID}
    auth-server-url: ${KAKAO_AUTH_URL}
    resource-server-url: ${KAKAO_USER_URL}
    secret-key: secret-key

  naver:
    client-id: ${NAVER_CLIENT_ID}
    auth-server-url: ${NAVER_AUTH_URL}
    resource-server-url: ${NAVER_USER_URL}
    secret-key: ${NAVER_SECRET_KEY}
jwt:
  secret: ${JWT_SECRET}
